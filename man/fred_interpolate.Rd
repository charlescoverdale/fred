% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{fred_interpolate}
\alias{fred_interpolate}
\title{Fill missing values in a FRED series}
\usage{
fred_interpolate(data, method = c("locf", "linear"))
}
\arguments{
\item{data}{A \code{fred_tbl} or \code{data.frame} with a \code{date} column.}

\item{method}{Character. \code{"locf"} (default) carries the last observed
value forward. \code{"linear"} does linear interpolation between adjacent
non-\code{NA} values.}
}
\value{
A \code{fred_tbl} with \code{NA}s filled.
}
\description{
Fills \code{NA} values in the \code{value} column (long format) or in numeric columns
(wide format). Two methods are supported: last-observation-carry-forward
(\code{"locf"}) and linear interpolation between adjacent observed values
(\code{"linear"}). Use this for mixed-frequency analysis where a low-frequency
series needs to be interpolated to a higher frequency.
}
\examples{
\donttest{
op <- options(fred.cache_dir = tempdir())
\dontrun{
  gdp <- fred_series("GDPC1", from = "2020-01-01")
  gdp_monthly <- fred_interpolate(gdp, method = "linear")
}
options(op)
}
}
\seealso{
Other utilities: 
\code{\link{fred_aggregate}()},
\code{\link{fred_event_window}()}
}
\concept{utilities}
