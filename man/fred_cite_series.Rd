% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cite.R
\name{fred_cite_series}
\alias{fred_cite_series}
\title{Generate a citation for a FRED series}
\usage{
fred_cite_series(
  series_id,
  vintage_date = NULL,
  format = c("bibtex", "text", "bibentry"),
  fetch_metadata = FALSE
)
}
\arguments{
\item{series_id}{Character. A single FRED series ID.}

\item{vintage_date}{Optional character or Date. Vintage date the data was
accessed as of. Defaults to today's date (the "accessed" date).}

\item{format}{Character. One of \code{"bibtex"} (default), \code{"text"}, or
\code{"bibentry"} (an R \code{utils::bibentry} object).}

\item{fetch_metadata}{Logical. If \code{TRUE}, call \code{\link[=fred_info]{fred_info()}} to fetch the
official series title. Requires an API key. Default \code{FALSE}.}
}
\value{
A character string (\code{"bibtex"}, \code{"text"}) or a \code{bibentry} object.
}
\description{
Produces a citation string for a single FRED series in BibTeX, plain text,
or \code{bibentry} form. Works offline by default: the citation falls back to
the series ID as the title. Pass \code{fetch_metadata = TRUE} to call
\code{\link[=fred_info]{fred_info()}} and use the official series title (requires an API key).
}
\details{
Cite a specific vintage by passing \code{vintage_date}. This is essential for
reproducible research: a 2023 GDP figure published in October 2023 may
differ materially from the same observation as it appears today.
}
\examples{
# BibTeX without an API call
fred_cite_series("GDPC1")

# Plain-text citation pinned to a vintage date
fred_cite_series("UNRATE", vintage_date = "2024-06-01", format = "text")

\dontrun{
  # Use the official title (requires an API key)
  fred_cite_series("GDPC1", fetch_metadata = TRUE)
}
}
\seealso{
Other reproducibility: 
\code{\link{fred_manifest}()},
\code{\link{fred_vintage_revisions}()}
}
\concept{reproducibility}
