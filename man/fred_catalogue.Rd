% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalogue.R
\name{fred_catalogue}
\alias{fred_catalogue}
\title{Browse a curated catalogue of popular FRED series}
\usage{
fred_catalogue(category = NULL, query = NULL)
}
\arguments{
\item{category}{Character. Optional category filter. One of \code{"GDP & Output"},
\code{"Employment"}, \code{"Inflation"}, \code{"Interest Rates"}, \code{"Housing"},
\code{"Financial"}, \code{"Money & Credit"}, \code{"Trade & FX"}, \code{"Consumer"},
\code{"Government & Fiscal"}. Multiple categories may be passed.}

\item{query}{Character. Optional free-text filter (e.g. \code{"unemployment"},
\code{"mortgage"}).}
}
\value{
A \code{fred_tbl} (a \code{data.frame} subclass) with columns \code{id}, \code{title},
\code{frequency}, \code{units}, \code{category}, \code{description}.
}
\description{
Returns a data frame of around 50 of the most widely used FRED series
organised by economic category. The catalogue is curated and embedded in
the package: no API call is made. Use it as a starting point for common
workflows or as a quick reference when you cannot remember a series ID.
}
\details{
Filter by category, by free-text query, or both. Free-text matches against
the series ID, title, and description (case-insensitive substring match).
}
\examples{
# Full catalogue
fred_catalogue()

# Inflation series only
fred_catalogue(category = "Inflation")

# Free-text search
fred_catalogue(query = "mortgage")

# Combined
fred_catalogue(category = "Interest Rates", query = "treasury")
}
\seealso{
Other catalogue: 
\code{\link{fred_browse}()}
}
\concept{catalogue}
