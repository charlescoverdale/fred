% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalogue.R
\name{fred_browse}
\alias{fred_browse}
\title{Browse the FRED category tree}
\usage{
fred_browse(category_id = 0L, depth = 1L)
}
\arguments{
\item{category_id}{Integer. Category to browse. Default \code{0} (root). The
root prints from a static reference and does not call the API.}

\item{depth}{Integer. How many levels deep to recurse. Default \code{1} (only
immediate children). Higher values trigger one API call per parent
category visited.}
}
\value{
A \code{fred_tbl} of categories at the requested level (invisibly).
}
\description{
Pretty-prints the FRED category tree. With no arguments, shows the eight
top-level FRED categories from a built-in static reference (no API call).
Pass a \code{category_id} to drill into its children, which fetches from the API
(and is cached). Use this to discover where series live before searching
inside a category with \code{\link[=fred_category_series]{fred_category_series()}}.
}
\examples{
\donttest{
# Top-level categories (no API call)
fred_browse()

op <- options(fred.cache_dir = tempdir())
# Drill into "National Accounts" (id 32992)
fred_browse(32992)
options(op)
}
}
\seealso{
Other catalogue: 
\code{\link{fred_catalogue}()}
}
\concept{catalogue}
