% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.fred_tbl}
\alias{plot.fred_tbl}
\title{Plot a fred_tbl}
\usage{
\method{plot}{fred_tbl}(
  x,
  recessions = TRUE,
  legend = TRUE,
  col = NULL,
  type = "l",
  main = NULL,
  xlab = "",
  ylab = "value",
  ...
)
}
\arguments{
\item{x}{A \code{fred_tbl}.}

\item{recessions}{Logical. If \code{TRUE} (default), shade NBER recession
periods that overlap the plotted date range.}

\item{legend}{Logical. If \code{TRUE} (default) and there are multiple series,
add a top-left legend.}

\item{col}{Character. Optional vector of colours, one per series. Default
uses a fixed six-colour qualitative palette, or HCL colours for >6 series.}

\item{type}{Character. Plot type, passed to \code{\link[graphics:lines]{graphics::lines()}}. Default
\code{"l"} (line).}

\item{main, xlab, ylab}{Plot labels. Sensible defaults are inferred.}

\item{...}{Other arguments passed to the initial \code{\link[graphics:plot.default]{graphics::plot()}} call.}
}
\value{
\code{x}, invisibly.
}
\description{
Time-series plot for a \code{fred_tbl}. Detects long or wide format, draws one
line per series, and optionally shades NBER recession periods. Uses base
graphics so no extra dependencies are pulled in.
}
\details{
For long-format input (\code{date}, \code{series_id}, \code{value}), one line per
\code{series_id}. For wide-format input (\code{date} plus one numeric column per
series), one line per numeric column.
}
\examples{
\donttest{
op <- options(fred.cache_dir = tempdir())
\dontrun{
  gdp <- fred_series("GDPC1", from = "2000-01-01")
  plot(gdp)

  panel <- fred_series(c("UNRATE", "CIVPART"), from = "2000-01-01",
                       format = "wide")
  plot(panel)
}
options(op)
}
}
