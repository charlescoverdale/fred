% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{fred_search}
\alias{fred_search}
\title{Search for FRED series}
\usage{
fred_search(
  query,
  type = "full_text",
  limit = 100L,
  order_by = "search_rank",
  filter_variable = NULL,
  filter_value = NULL,
  tag_names = NULL
)
}
\arguments{
\item{query}{Character. Search terms (e.g. \code{"GDP"}, \code{"unemployment rate"}).}

\item{type}{Character. Either \code{"full_text"} (default) for keyword search
or \code{"series_id"} for series ID substring matching (supports \code{*}
wildcards).}

\item{limit}{Integer. Maximum number of results to return. Default 100,
maximum 1000.}

\item{order_by}{Character. How to order results. One of \code{"search_rank"}
(default), \code{"series_id"}, \code{"title"}, \code{"units"}, \code{"frequency"},
\code{"seasonal_adjustment"}, \code{"last_updated"}, \code{"popularity"},
\code{"group_popularity"}.}

\item{filter_variable}{Character. Optional variable to filter by. One of
\code{"frequency"}, \code{"units"}, or \code{"seasonal_adjustment"}.}

\item{filter_value}{Character. The value to filter on (e.g. \code{"Monthly"},
\code{"Quarterly"}). Required if \code{filter_variable} is specified.}

\item{tag_names}{Character. Optional comma-separated tag names to filter
results (e.g. \code{"gdp"}, \code{"usa;quarterly"}).}
}
\value{
A data frame of matching series with columns including \code{id},
\code{title}, \code{frequency}, \code{units}, \code{seasonal_adjustment}, \code{last_updated},
\code{popularity}, and \code{notes}.
}
\description{
Searches the FRED database by keywords or series ID substring. Returns
matching series with their metadata, ordered by relevance.
}
\examples{
\donttest{
op <- options(fred.cache_dir = tempdir())
# Keyword search
fred_search("unemployment rate")

# Filter to monthly series only
fred_search("consumer price index", filter_variable = "frequency",
            filter_value = "Monthly")

# Search by series ID pattern
fred_search("GDP*", type = "series_id")
options(op)
}
}
\seealso{
Other series: 
\code{\link{fred_info}()},
\code{\link{fred_series}()},
\code{\link{fred_updates}()},
\code{\link{fred_vintages}()}
}
\concept{series}
