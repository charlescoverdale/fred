% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{fred_event_window}
\alias{fred_event_window}
\title{Extract data windows around event dates}
\usage{
fred_event_window(data, events, window = c(-30L, 90L))
}
\arguments{
\item{data}{A \code{fred_tbl} or \code{data.frame} with a \code{date} column.}

\item{events}{A character or Date vector of event dates.}

\item{window}{Integer length-2. Days before (negative or zero) and after
(positive or zero) each event date. Default \code{c(-30L, 90L)}.}
}
\value{
A \code{fred_tbl} with the original columns plus \code{event_date} and
\code{days_from_event}.
}
\description{
Given a \code{fred_tbl} (or any data frame with a \code{date} column) and a vector of
event dates, returns one row per (event, observation) pair where the
observation falls inside the requested window. Useful for event studies
around FOMC decisions, recession peaks, or release dates.
}
\details{
Long-format input with \code{series_id}/\code{value} is supported, as is wide-format
input from \code{fred_series(..., format = "wide")}. The window is in calendar
days; for monthly or quarterly data, choose a window large enough to
capture at least one observation.
}
\examples{
# Synthetic example — works offline
d <- seq(as.Date("2024-01-01"), as.Date("2024-12-31"), by = "month")
df <- data.frame(date = d, value = seq_along(d))
events <- as.Date(c("2024-03-15", "2024-09-15"))
fred_event_window(df, events = events, window = c(-30L, 60L))

\donttest{
op <- options(fred.cache_dir = tempdir())
# With live FRED data: UNRATE around 2024 SEP meetings (needs API key)
\dontrun{
  ur <- fred_series("UNRATE", from = "2023-01-01")
  sep <- fred_fomc_dates(year = 2024, sep_only = TRUE)
  fred_event_window(ur, events = sep$date, window = c(-60L, 60L))
}
options(op)
}
}
\seealso{
Other utilities: 
\code{\link{fred_aggregate}()},
\code{\link{fred_interpolate}()}
}
\concept{utilities}
