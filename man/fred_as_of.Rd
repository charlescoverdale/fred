% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vintages.R
\name{fred_as_of}
\alias{fred_as_of}
\title{Fetch a series as it appeared on a given vintage date}
\usage{
fred_as_of(
  series_id,
  date,
  from = NULL,
  to = NULL,
  units = "lin",
  frequency = NULL,
  aggregation = "avg",
  cache = TRUE
)
}
\arguments{
\item{series_id}{Character. One or more FRED series IDs.}

\item{date}{Character or Date. The vintage date (\code{"YYYY-MM-DD"}).}

\item{from, to}{Optional observation date range.}

\item{units}{Character. Raw FRED units code. Default \code{"lin"}.}

\item{frequency, aggregation}{Optional frequency aggregation arguments
(see \code{\link[=fred_series]{fred_series()}}).}

\item{cache}{Logical. Cache results locally. Default \code{TRUE}.}
}
\value{
A \code{fred_tbl} with columns \code{date}, \code{series_id}, \code{value},
\code{realtime_start}, \code{realtime_end}.
}
\description{
Returns the values that were available in FRED on \code{date}, before any
subsequent revisions. This is the standard real-time data access pattern:
set \code{realtime_start = realtime_end = date}. Useful for backtesting
forecasting models against the data that was actually available at the
time, not the revised series we see today.
}
\details{
Underneath, this calls the \code{series/observations} endpoint with the
realtime parameters set. Results are cached separately from the
default (latest-vintage) cache, so calling \code{fred_series("GDP")} and
\code{fred_as_of("GDP", "2020-01-15")} keep distinct cache entries.
}
\examples{
\donttest{
op <- options(fred.cache_dir = tempdir())
# GDP as it looked on 1 March 2020
gdp_2020 <- fred_as_of("GDP", "2020-03-01")
options(op)
}
}
\seealso{
Other vintages: 
\code{\link{fred_all_vintages}()},
\code{\link{fred_first_release}()},
\code{\link{fred_real_time_panel}()}
}
\concept{vintages}
