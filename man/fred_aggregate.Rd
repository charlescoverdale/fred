% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utilities.R
\name{fred_aggregate}
\alias{fred_aggregate}
\title{Aggregate FRED observations to a coarser frequency}
\usage{
fred_aggregate(data, fun = "mean", by = "month")
}
\arguments{
\item{data}{A \code{fred_tbl} or \code{data.frame} with a \code{date} column.}

\item{fun}{Character. Aggregation function. One of \code{"mean"}, \code{"sum"},
\code{"first"}, \code{"last"}, \code{"median"}, \code{"min"}, \code{"max"}. Default \code{"mean"}.}

\item{by}{Character. Target frequency. One of \code{"week"}, \code{"month"},
\code{"quarter"}, \code{"year"}. Default \code{"month"}.}
}
\value{
A \code{fred_tbl} with the same columns as the input, with \code{date}
collapsed to period start.
}
\description{
Aggregates a long-format \code{fred_tbl} (with \code{date}, \code{series_id}, \code{value}) or
a wide-format \code{fred_tbl} (date plus one column per series) to a coarser
calendar frequency. For long format, aggregation is performed per
\code{series_id}; for wide format, per numeric column.
}
\details{
Use this when you have, say, daily Treasury yields and need a monthly
average, or weekly initial claims aggregated to monthly totals. For
server-side aggregation that mirrors FRED's own interpolation conventions,
pass \code{frequency = "m"} to \code{\link[=fred_series]{fred_series()}} instead.
}
\examples{
\donttest{
op <- options(fred.cache_dir = tempdir())
\dontrun{
  daily_yields <- fred_series("DGS10", from = "2023-01-01")
  monthly_yields <- fred_aggregate(daily_yields, fun = "mean", by = "month")
}
options(op)
}
}
\seealso{
Other utilities: 
\code{\link{fred_event_window}()},
\code{\link{fred_interpolate}()}
}
\concept{utilities}
