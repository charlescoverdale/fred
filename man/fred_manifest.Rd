% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/manifest.R
\name{fred_manifest}
\alias{fred_manifest}
\title{Snapshot a session's FRED downloads as a YAML manifest}
\usage{
fred_manifest(..., file = NULL)
}
\arguments{
\item{...}{\code{fred_tbl} objects (positional or named) or a single list of
\code{fred_tbl} objects.}

\item{file}{Optional path to write the YAML manifest. If \code{NULL} (default),
the manifest is returned as a \code{fred_manifest} object (printable).}
}
\value{
A \code{fred_manifest} object (a character string with an attached
print method). If \code{file} is supplied, written to disk and returned
invisibly.
}
\description{
Produces a YAML manifest describing one or more \code{fred_tbl} objects, with
query metadata, dimensions, date ranges, and an MD5 hash of each object.
Intended to be saved alongside paper code for reproducibility checks: if
the manifest still hashes to the same values, the data underlying the
analysis has not changed.
}
\details{
Pass \code{fred_tbl} objects positionally, named, or as a list. If passed a
bare list, names from the list are used; otherwise objects are labelled
\code{obj_1}, \code{obj_2}, ...
}
\examples{
\donttest{
op <- options(fred.cache_dir = tempdir())
\dontrun{
  gdp <- fred_series("GDPC1", from = "2020-01-01")
  un  <- fred_series("UNRATE", from = "2020-01-01")
  m <- fred_manifest(gdp = gdp, unrate = un)
  print(m)
  fred_manifest(gdp = gdp, file = file.path(tempdir(), "manifest.yml"))
}
options(op)
}
}
\seealso{
Other reproducibility: 
\code{\link{fred_cite_series}()},
\code{\link{fred_vintage_revisions}()}
}
\concept{reproducibility}
